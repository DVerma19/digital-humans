# Digital Humans

A deterministic, inspectable, procedurally generated civilization simulation.

See `docs/ARCHITECTURE.md` for the full baseline.

## Build

    cmake --preset debug
    cmake --build --preset debug
    ctest --preset debug